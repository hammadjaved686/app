import { MycapitalizePipePipe } from './my-capitalize-pipe.pipe';

describe('MycapitalizePipePipe', () => {
  it('create an instance', () => {
    const pipe = new MycapitalizePipePipe();
    expect(pipe).toBeTruthy();
  });
});
